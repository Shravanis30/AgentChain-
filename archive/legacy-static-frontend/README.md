# Legacy Static Frontend Prototype

This directory preserves the original pre-Next.js static prototype (`index.html`, `app.js`, `style.css`) for historical and grading reference.

The production web client is located at [`/frontend`](../../frontend).
